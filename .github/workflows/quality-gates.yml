name: Quality Gates

on:
  pull_request:
  push:
    branches:
      - main
      - development

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Rule contracts
        run: pnpm check:rule-contracts

      - name: Headless quality gates
        run: pnpm --filter @lyfie/luthor-headless lint test build

      - name: Luthor quality gates
        run: pnpm --filter @lyfie/luthor lint test build

      - name: Release hardening smoke
        run: pnpm check:release-hardening
