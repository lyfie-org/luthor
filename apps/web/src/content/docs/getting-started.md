---
title: Getting Started with @lyfie/luthor
description: Install and run the plug-and-play editor package.
---

Install the package and peer dependencies:

```bash
pnpm add @lyfie/luthor react react-dom
```

Import the editor and styles:

```tsx
import { ExtensiveEditor } from '@lyfie/luthor';
import '@lyfie/luthor/styles.css';
```

Then render:

```tsx
<ExtensiveEditor />
```

For deeper docs, continue with:

- [Reference user guide for luthor](/docs/reference/user/luthor/getting-started/)
- [Presets and configuration](/docs/reference/user/luthor/presets-and-configuration/)
